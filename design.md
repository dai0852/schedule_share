# デザインシステム (studio-csaスタイル)

このアプリケーションは、[studio-csa.com](https://www.studio-csa.com/) のビジュアルアイデンティティを参考にした「シンプル＆ポップ・方眼紙（マス目）」スタイルを採用しています。

## 1. カラーパレット (Colors)

- **メインアクセント (CSA Green)**: `#00ac94`
  - ボタンの背景や重要なラベル、ホバー時のアクセントなどに使用します。サイトを象徴する清潔感とポップさを兼ね備えた色です。
- **ベースカラー**: `#ffffff` (White), `#000000` (Black)
  - 余白をたっぷりと取り、基本は白地に黒の細い線で構成します。
- **背景方眼紙色**: `#e5e7eb` (tailwindの gray-200 相当など、非常に薄いグレーや水色)

## 2. 形状・境界線 (Shapes & Borders)

- **角丸 (Border Radius)**:
  - コンテナー/カード部分: `border-radius: 40px` (`rounded-[40px]`)
  - ボタン等: カプセル型 (`rounded-full` あるいは `rounded-[999px]`)
- **境界線 (Border)**:
  - 全ての主要カードやボタンには、極細の黒線 `border border-black` または `border-2 border-black` を使用します。
  - 太すぎる線は避け、繊細でクリーンな印象を保ちます。
- **影 (Shadows)**:
  - 強いオフセットの影（Shadow Neo）は使わず、基本は影なし（フラット）か、必要最低限のごく薄い影に留めます。

## 3. 背景 (Backgrounds)

- **マス目（方眼紙・グリッド）**:
  - `body` 全体、あるいはメイン領域の背景にはCSSグラデーションで「方眼紙（グラフペーパー）」のような薄いマス目を描画します。
  - 実装例: `linear-gradient` 等を用いた 15px〜30px 程度のマス目。

## 4. タイポグラフィ (Typography)

- ゴシック体を使用し、見出しやボタンなどの短いテキストには太字 (`font-bold`, `font-black`) を多用します。
- `tracking-wide` (文字間隔を広め) にすることで、空気感のあるポップさを演出します。
- 文字は十分に大きく、読みやすく設定します。

## 5. UIの振る舞い (Interactions)

- ボタンホバー時などには、背景色がグリーンから白（あるいは黒）に反転したり、位置が微細に動く (`hover:-translate-y-1` な軽やかなアニメーション) 演出を入れ、操作性を高めます。
